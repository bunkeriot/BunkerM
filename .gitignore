# Environment & Secrets
.env
.env.local
!.env.example
!.env.local.example

# Python
__pycache__/
*.py[cod]
venv/
.venv/

# Logs
*.log

# SSL / Certificates
ssl_certificates/*.pem
ssl_certificates/*.key
ssl_certificates/*.crt
*.pem
*.key

# Application data
backend/logs/
backend/app/*/logs/
dynamic-security.json
*.json.backup

# Backup files (may contain secrets)
*.bak
*.backup

# BunkerAI Cloud runtime data (contains API keys — never commit)
bunkerai_config.json
.bunkerai_status.json

# Internal documentation (not for public distribution)
ARCHITECTURE.md
API_DOCUMENTATION.md
